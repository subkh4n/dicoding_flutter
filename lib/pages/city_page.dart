import 'package:flutter/material.dart';
// import 'package:shamo/models/city.dart';

class CityPage extends StatelessWidget {
  // final City city;
  const CityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page City'),
      ),
      body: Center(
          // child: Text(),
          ),
    );
  }
}
